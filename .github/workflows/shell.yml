name: Shell Script
on:
  workflow_dispatch:
jobs:
  shell_script:
    runs-on: ubuntu-latest
    steps:
      - name: Run a shell script
        run: |
          cat /proc/$$/cmdline
          script=$0
          echo "script=${script}"
          cat ${script}
