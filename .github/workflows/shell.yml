name: shell script
on:
  workflow_dispatch:
jobs:
  shell_script:
    runs-on: ubuntu-latest
    steps:
      - name: Run a shell script
        run: |
          echo "PID=$$"
          ps auxwww | grep "$$"
